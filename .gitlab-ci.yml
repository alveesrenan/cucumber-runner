stages:
  - registry
  - test

cache:
  untracked: true
  key: "$CI_COMMIT_REF_NAME"

build:
  image: docker:latest
  stage: registry
  services:
      - docker:dind
  script:
    - sh build.sh

test:
  image: registry.atech.com.br/cucumber-runner
  stage: test
  before_script:
    - cd test/
    - gem install bundler
    - bundle install
  script:
    cucumber